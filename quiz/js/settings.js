/* let  effectsVolume = 0.4;
const volumeScale = document.querySelector('.volume-scale');
volumeScale.addEventListener('change', e => {
  effectsVolume = e.target.value / 100;
});

// export default { effectsVolume, volumeScale};  
let aax = 10;
let aax2 = 101;

export {aax, aax2, effectsVolume, volumeScale};
 */
const textObj = {
  testF: [1,2],
}
export default textObj;